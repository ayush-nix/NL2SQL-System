# NL2SQL — Air-Gapped Dependencies
# Core web framework
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# LLM interaction (Ollama REST API)
httpx>=0.25.0

# SQL parsing and validation
sqlglot>=20.0.0

# Templating (FastAPI optional dependency)
jinja2>=3.1.0
